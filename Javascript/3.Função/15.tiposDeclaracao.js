console.log(soma(4, 3));

//Function Declaration (+ utilizada e flexível)

function soma(x, y) {
    return x + y
};

//Function Expression  (+ utilizada)

const subtracao = function(x, y) {
    return x - y
};

console.log(subtracao(4, 3));

//Function Named Expression (- utilizada)

const multiplicacao = function multiplicacao(x, y) {
    return x * y
};

console.log(multiplicacao(4, 3));